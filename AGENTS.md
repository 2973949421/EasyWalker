# ADV Walkman Agent 工作规约

这份文档是给 Codex / 后续 Agent 的项目专用操作规约。

它不替代 `docs/PRD.md`、`docs/TECH_DESIGN.md`、`TASKS.md` 或其他项目文档；它只规定 Agent 在本项目中应如何阅读、计划、修改、验证和处理硬件。

---

## 1. 沟通与文档定位

默认使用中文沟通。

工程术语、模块名或缩写首次出现时，尽量写成：

```text
English（中文）
```

或：

```text
中文（English）
```

例如：

- Backend（底层实现）
- Buffer（缓冲区）
- Underrun（缓冲欠载）
- Acceptance Criteria / AC（验收标准）

代码标识符、路径、命令、API、类名和字段名保持原样，不强行翻译。

`AGENTS.md` 只定义开发工作方式：

- 不在这里重复 PRD；
- 不在这里重复完整技术架构；
- 不在这里维护 Backlog；
- 不在这里记录具体版本变更。

---

## 2. Source of Truth（事实依据）

项目文档优先级：

```text
产品目标与范围
→ docs/PRD.md

正式技术方案
→ docs/TECH_DESIGN.md

当前任务与 AC
→ TASKS.md

音频底层实验和选型结果
→ docs/AUDIO_BENCHMARK.md

开源与官方资料研究
→ docs/REFERENCES.md

Agent 工作方式
→ AGENTS.md
```

`docs/REFERENCES.md` 是参考资料，不是最终技术决策。最终采用方案以 `docs/TECH_DESIGN.md` 和已经完成的真机验证结果为准。

涉及 Cardputer ADV 硬件事实时，优先级为：

```text
M5Stack 官方资料 / 原理图 / 官方源码
↓
芯片 Datasheet（数据手册）
↓
本项目真机验证结果
↓
高质量开源项目
↓
推断
```

不要把推断写成事实。

如果真机行为和原有预期冲突，以真机现象作为新的调查起点，重新核对代码、文档和硬件条件。

---

## 3. 效率优先与工程原则

本项目的默认原则：

> 不过度设计，不要过度防御，不要把普通开发任务做成电脑攻防大战。以效率和实际收益为优先。

具体规则：

- 优先实现能验证当前目标的最小版本。
- 不为理论上的极低概率风险增加大量流程、抽象或检查。
- 一个可靠验证已经足够时，不机械重复备份、Hash、全量扫描或全量构建。
- 不因为“更保险”就重新读取完整 Flash、重建环境或重装工具链。
- 不在没有实际问题时修改 M5Burner、Bootloader、Partition Table（分区表）或完整 Flash。
- 一个方法失败后先读错误、判断原因，再做最小针对性检查；不要机械重复。
- 不把与当前任务无关的重构混入工作。
- 允许重构，但重构必须直接服务于当前任务、稳定性、可维护性或已确认的后续需求。
- 不因为“可以增加抽象”就增加抽象。
- 已有成熟方案可借鉴时，优先研究和复用其有效设计，避免从零重复造轮子。
- 不为了假想的未来需求提前建设大型插件系统、框架或防御体系。
- 防御性措施和验证成本应与真实风险相匹配。
- 正常 firmware 开发不是数字取证，也不是灾难恢复演练。

---

## 4. Plan Mode（计划模式）

当用户明确说：

- “先规划”
- “先别写”
- “看看怎么做”
- “给方案”
- “做设计”
- “先评估”

时，不立即修改文件。

计划模式的目标不是写长篇路线图，而是让用户能判断：

```text
Agent 理解问题了吗？
Agent 打算怎么改？
Agent 怎么证明改对了？
```

### 有效计划默认包含

1. **目标**
   - 要解决什么问题；
   - 最终希望得到什么结果。

2. **当前状态**
   - 相关代码、文档、已有实现和已知限制；
   - 如涉及代码修改，先确认工作区状态。

3. **范围**
   - 本次 In Scope（会做什么）；
   - 本次 Out of Scope（不会顺手做什么）。

4. **实现方案**
   - 采用什么技术路径；
   - 是否借鉴已有开源实现；
   - 为什么选择这条路径。

5. **预计改动**
   - 会检查、修改或新增哪些主要文件 / 模块；
   - 这是预期，不要伪装成已经确认的事实。

6. **验证方式**
   - 自动化验证；
   - 如果涉及硬件，真机如何验收；
   - 什么现象代表成功或失败。

7. **阻塞问题**
   - 只提出真正阻塞执行或涉及重大产品/技术取舍的问题。

复杂任务可补充：

- 风险与未知项；
- 备选方案；
- 分阶段实施。

简单任务不需要机械套完整模板。

用户在计划过程中补充约束时，要把新信息吸收到原目标中，不要被个别例子带偏。

---

## 5. 仓库、Git 与修改纪律

开始代码、文档或配置修改前，先运行：

```bash
git status --short
```

规则：

- 不覆盖、回滚或格式化与当前任务无关的用户改动。
- 相关文件已有修改时，先读清楚再继续。
- 先读 `TASKS.md` 中当前任务和 AC。
- 只读取当前任务真正需要的文档和源码，不要每次从头研究整个仓库。
- `docs/REFERENCES.md` 已有充分结论时，不重复做一轮互联网考古，除非现有结论不足、已经过时或用户明确要求重新验证。

### Commit

Codex 可以自行 Commit。

建议在完成一个有意义且已经通过相应验证的工作单元后提交，例如：

```text
完成一个 backend
→ build / test
→ commit
```

不要：

- 每改几行就制造一个 Commit；
- 为了“漂亮历史”制造大量机械提交；
- 自动覆盖用户未提交的修改。

Push 不作为默认动作，除非任务或用户明确要求。

---

## 6. Embedded / Hardware（嵌入式与硬件规则）

### 6.1 不猜硬件

以下内容不确定时，必须查官方资料、源码、数据手册或已有真机记录：

- GPIO
- 供电与电平
- I2S 参数
- ES8311 寄存器
- microSD 引脚
- Flash offset
- Partition
- Download Mode
- 外部模块接线

不要根据“常见 ESP32 开发板”推断 Cardputer ADV。

### 6.2 Build Success ≠ Device Validation

必须明确区分：

```text
Build Success（编译成功）
≠
Device Validation（真机验收通过）
```

`pio run` 成功只能证明代码通过构建，不能证明：

- MP3 320 kbps 稳定播放；
- 没有爆音；
- UI 不干扰音频；
- 按键手感正常；
- EQ 没有削波；
- microSD 浏览不卡音。

硬件相关任务的 AC 应尽量分成：

```text
Automatic Validation（自动验证）
Device Validation（真机验收）
```

### 6.3 Flash 权限

Codex 有权限进行正常的：

- Build
- Flash
- Serial Monitor
- M5Launcher 正常安装 / 返回路径测试
- 当前 firmware 的真机测试

但以下情况必须停止并向用户报告，不要一路扩大操作：

- 需要猜 Flash offset；
- 需要修改 Partition Table；
- 需要 full-flash erase；
- 需要恢复整个 8MB Flash；
- 需要重新读取完整 Flash 做“保险备份”；
- Bootloader 行为异常；
- 设备识别结果和预期明显冲突；
- 想通过修改 M5Burner 来“修”普通固件问题。

禁止绕过 Codex / 平台自身的高风险权限机制。

---

## 7. Environment & Dependencies（环境与依赖）

优先使用当前已工作的 PlatformIO / ESP32 开发环境。

默认规则：

- 正常 build / test 不主动升级 PlatformIO Core。
- 不为了“最新版”主动升级 ESP32 platform 或 Arduino framework。
- 不因为单个 build error 就删除缓存或重装整个工具链。
- 不用重装依赖替代问题定位。
- 依赖出现异常时，先判断是否真的是环境问题。

Codex 允许自主：

- 新增依赖；
- 删除依赖；
- 调整依赖版本；
- 做合理重构。

前提是：

- 直接服务当前任务；
- 有明确收益；
- 不只是“追新”或架构洁癖。

如果某一技术路线确实需要不同底层环境，例如 BackgroundAudio 需要较新的 IDF5 / pioarduino，则：

- 作为独立实验环境处理；
- 不直接污染当前稳定主环境；
- 实验结果足够好后再决定是否迁移主线。

---

## 8. 自主判断、验证与停止条件

### 8.1 小问题自行解决

能通过以下方式解决的问题，不要立即丢回给用户：

- 读代码；
- 读项目文档；
- 查官方资料；
- 查现有参考项目；
- 运行一个小范围实验；
- 修复普通编译错误；
- 调整小范围参数。

例如：

- include 路径错误；
- 库 API 用法；
- 一个 buffer 参数；
- 某函数定义位置；
- 官方 pin 定义；
- 普通 build error。

### 8.2 大问题与用户沟通

出现以下情况应和用户讨论：

- 需要改变已确认的 PRD；
- 需要改变用户确认后的 UI / Keymap；
- 要放弃 V1 已确认能力；
- 真机证明硬件无法实现既定目标；
- 多个方案都成立，但代表明显不同的产品体验；
- 技术选型存在重大 trade-off（取舍），仅靠工程指标无法决定。

原则：

> 技术细节尽量自行消化；产品选择和重大取舍与用户讨论。

### 8.3 按改动范围做最小但有效的验证

```text
改 Audio
→ build + 对应自动测试 + 真机耳机验证

改 Library
→ build + 大目录测试 + 播放中浏览测试

改 UI
→ build + 真机显示与按键验收

改 DSP
→ build + 削波/性能检查 + 同音源 A/B

改 Launcher
→ Launcher 安装 / 返回路径验收

改纯文档
→ 检查内容和链接，不为了仪式感重新刷 firmware
```

不要把全量 build 当作每个小改动的唯一证明。

### 8.4 失败处理顺序

遇到失败时：

```text
1. 读错误
2. 判断更像代码、环境还是硬件问题
3. 做最小针对性检查
4. 修正并重新验证
5. 确认主路径不成立后，再考虑替代方案
```

禁止默认演变成：

```text
失败
→ 重装 PlatformIO
→ 重读 8MB Flash
→ 重新做多轮 Hash
→ 重建分区
→ 修改 M5Burner
→ 最后才发现只是普通代码问题
```

如果连续尝试已经没有新增信息，停止并总结已知事实，不要用更多操作制造噪声。

---

## 9. 当前阶段执行约束

当前执行阶段和状态一律从 `TASKS.md` 路由，不在本文件维护容易过期的任务快照。

P0 已经完成并冻结 V1 Audio Backend：

```text
ESP8266Audio 1.9.7
→ 32-bit Stereo to Mono downmix
→ 3 × 1536-sample M5.Speaker buffer
→ M5Unified Cardputer ADV / ES8311
```

P0 选定的 Candidate A / M5.Speaker Backend 不变。P2 真机播放中浏览暴露原
`3 × 768` 仅约 52 ms 的缓冲余量不足，因此正式 Player 将单 Buffer 调整为
1536 samples；这是已确认主路径内的稳定性校准，不是重新开启 Backend 选型。

除非后续真机证据证明该主路径不可用，否则不要重新开启 A/B/C 选型，也不要把 Candidate B/C 的实验工具链带入正式 Player。

V1 Sound Preset、Keymap 与主要 UI/UX 已完成产品侧设计。字号、列宽、ASCII 网格与音效细节允许真机小范围校准，但不应被当作重新做产品设计的理由。正式 Player Core 阶段不抢先实现大型 UI、Library、歌词、ASCII Cover 或 DSP。
